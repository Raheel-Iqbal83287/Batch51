let users2 = ['Ali', 'Admin', 'Zahid', 'Bashir', 'Sarim']

if (users2.length == 0)
{
    console.log("We need to find some users.")
}

users2 = []

if (users2.length == 0)
{
    console.log("We need to find some users.")
}

